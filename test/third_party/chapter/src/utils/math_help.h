#ifndef THIRDPARTY_CHAPTER_SRC_UTILS_MATH_HELP_H
#define THIRDPARTY_CHAPTER_SRC_UTILS_MATH_HELP_H

#define MIN(a, b)   ((b) < (a) ? (b) : (a))
#define MAX(a, b)   ((b) > (a) ? (b) : (a))

#endif // THIRDPARTY_CHAPTER_SRC_UTILS_MATH_HELP_H
